test:
  override:
    - html5validator
deployment:
  production:
    branch: master
    commands:
      - rm -rf $HOME/$CIRCLE_PROJECT_REPONAME/.git
      - turbolift -u $RAXUSER -a $RAXAPIKEY --os-rax-auth $RAXREGION upload --sync --delete-remote -s $HOME/$CIRCLE_PROJECT_REPONAME -c $CONTAINER
